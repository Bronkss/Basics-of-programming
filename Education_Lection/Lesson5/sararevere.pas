program SarahRevere(input, output);
var 
    w1, w2, w3, w4, Looking: char;
begin {SarahRevere}
    {Инициализация}
    while Looking = 'Y' do 
    begin {while Looking = 'y'}

    end; {while Looking = 'y'}
    
end. {SarahRevere}